export const validateStructure = (argumentList) => {

}

const checkIfThereHeaderAndSidebar = (argumentList) => {
    // Check if the structureFile has a header and sidebar
}

checkPageIsCorrectlyDefined = (argumentList) => {
    // Check if the structureFile has a page correctly defined
}

